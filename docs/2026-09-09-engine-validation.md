# Phase 3 事件引擎真实数据验证报告

日期：2026-09-09 | 输入：GUI worker 真实管线输出 | 运行：`python tools/validate_engine_on_tracks.py`

## 验证目的

事件引擎（`frisbee_analyzer/events.py`）此前只有合成轨迹单测（6 项）。本验证检查它在**真实检测/跟踪/分队数据**上的行为，并明确其前置条件。

## 输入数据

| 项 | 值 |
|---|---|
| 文件 | `.worktrees/gui-v0/runs/gui_analysis/accept_10min/tracks.json` |
| 帧数 | 18,004（10 分钟，30fps） |
| 检测总数 | 396,983 |
| 带队属检测 | 268,767（68%） |
| 分队颜色 | team 0=red / team 1=blue（正确） |
| 飞盘检测 | **无**（该 run 只跑球员管线，`--weights yolo26x` 为 COCO person） |

## 结果

| 事件类型 | 引擎输出 | E6 自动 GT（整场 47min） |
|---|---|---|
| possession_start | 0 | — |
| transfer（交换手） | 0 | — |
| turnover | 0 | 1 |
| score | 0 | 14 |
| disc_on_ground | 71 | — |

**引擎计数：71（全部为 disc_on_ground）**

## 结论（三条，均为实测）

1. **引擎保守、无幻觉**：在缺少真实飞盘轨迹时，它**没有产生任何虚假的持盘/交换手/得分事件**——71 个 `disc_on_ground` 全部来自“无持有者 + 低速持续 N 帧”的判定，符合设计。这是好消息：误报代价高的事件统计不会凭空生成。

2. **但事件统计的硬前提是飞盘轨迹**：持盘判定要求“盘中心落在球员手部区域 + 盘速低于阈值”，而本次输入无飞盘检测（用最高置信度球员代理，盘位置永远在球员身上且随球员移动 → 速度不为 0 → 不满足持盘条件）。**结论：Phase 3 事件统计必须先接入飞盘检测/跟踪管线**（当前 `iter_player_tracks` 只检测 `classes=[0]` 即 person）。

3. **前置条件清单**（据此排期）：
   - ① 飞盘检测接入 worker（`--weights` 支持飞盘模型 + 类别配置）——**最高优先级**
   - ② 单应性标定接入（本验证用画面线性代理，精度不足；真实测速/得分区判定需标定 json）
   - ③ 得分区多边形与攻防方向配置（引擎已支持 `EndZone`，待标定后填入）
   - ④ 用 E6 GT（14 得分/1 攻转）做端到端对齐评测（±90s 容差）

## 产物

- `results/engine_validation.json`（完整事件列表与统计）
- 本报告

## 下一步（非 GPU 可继续的部分）

- ①③ 的代码改造可在无 GPU 下完成（worker 加飞盘类别参数 + 标定 json 读取）
- ④ 的对齐评测脚本可先写好，等飞盘管线跑出真实轨迹即可执行
